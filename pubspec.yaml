name: abayazeed_link_manager
description: أبايذيد لنك منجر — إدارة شبكات Starlink & MikroTik
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true

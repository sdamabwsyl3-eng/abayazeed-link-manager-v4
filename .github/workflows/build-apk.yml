name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          channel: 'stable'

      - name: 🔄 إنشاء مشروع فلاتر نظيف (يصلح ملفات الأندرويد تلقائيًا)
        run: |
          flutter create --org com.abayazeed.link_manager . --platforms android --no-pub
          flutter pub get

      - name: Build APK
        run: flutter build apk --release --no-tree-shake-icons

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: abayazeed-link-manager-APK
          path: build/app/outputs/flutter-apk/app-release.apk
